[project]
name = "blog"
version = "0.1.0"
description = "diarium electronicum de uukelele"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-git-revision-date-localized-plugin>=1.5.1",
    "zensical>=0.0.34",
]
